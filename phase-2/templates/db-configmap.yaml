apiVersion: v1
kind: ConfigMap
metadata:
  name: db-config
data:
  POSTGRES_DB: {{ .Values.django.env.DATABASE_NAME }}
  POSTGRES_USER: {{ .Values.django.env.DATABASE_USER }}
  # Add additional DB configuration as needed
