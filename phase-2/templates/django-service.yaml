apiVersion: v1
kind: Service
metadata:
  name: django-service
spec:
  selector:
    app: django-app
  ports:
    - protocol: TCP
      port: {{ .Values.django.service.port }}
      targetPort: {{ .Values.django.service.port }}
  type: ClusterIP
